spring.application.name=microservicio-auth

# Config basada en variables de entorno (pensada para K8s)
# Si no se establecen, se usan valores por defecto razonables para ejecución local

# CONFIG KUBERNETES (MINIKUBE)
# spring.datasource.url=jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_DATABASE:genosentinel_auth}
# spring.datasource.username=${DB_USERNAME:root}
# spring.datasource.password=${DB_PASSWORD:1234}
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# El secreto real viene por la variable de entorno JWT_SECRET

# CONFIG LOCAL (XAMPP)
spring.datasource.url=jdbc:mysql://localhost:3306/genosentinel_auth
spring.datasource.username=root
spring.datasource.password=hola1234
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# El secreto real viene por la variable de entorno JWT_SECRET (opcional en local)