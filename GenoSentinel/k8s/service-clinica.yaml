apiVersion: v1
kind: Service
metadata:
  name: microservicio-clinica
  namespace: genosentinel
spec:
  type: ClusterIP
  selector:
    app: microservicio-clinica
  ports:
    - name: http
      port: 3001
      targetPort: 3001
